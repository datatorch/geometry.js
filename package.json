{
  "name": "@datatorch/geometry",
  "version": "0.0.9",
  "description": "Geometry libary for JavaScript",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": "https://github.com/datatorch/geometry.js.git",
  "author": "jsbroks <jsbroks@gmail.com>",
  "license": "MIT",
  "scripts": {
    "update": "yarn upgrade-interactive --latest",
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc",
    "test": "jest",
    "doc:generate": "rm -rf docs && typedoc",
    "changelog": "conventional-changelog -i CHANGELOG.md -s -r 2 && git add CHANGELOG.md",
    "release": "yarn build && yarn publish && yarn changelog"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "testRegex": "^.+\\.spec\\.ts$"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.13",
    "conventional-changelog-cli": "^2.0.34",
    "jest": "^26.0.1",
    "prettier": "^2.0.5",
    "standard-changelog": "^2.0.24",
    "ts-jest": "^26.1.0",
    "ts-node": "^8.10.2",
    "typedoc": "^0.17.7",
    "typedoc-plugin-markdown": "^2.3.1",
    "typescript": "^3.9.5"
  },
  "dependencies": {}
}
